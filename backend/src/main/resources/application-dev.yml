server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:nasdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: false

app:
  storage:
    dir: ./nas-data
  ffmpeg:
    path: ffmpeg
  jwt:
    secret: ${JWT_SECRET:c2VjdXJlLXNlY3JldC1rZXktZm9yLW5hcy1iYWNrZW5kLXByb2plY3QtMjAyNg==}
